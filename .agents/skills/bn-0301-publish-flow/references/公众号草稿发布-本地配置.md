# 公众号草稿发布本地配置

本文件含密钥，不进 git，由用户自己创建。未创建时，agent 必须向用户索要 base 地址与 token，不得臆造。

```bash
export BASE_URL="https://<后端地址>"
export TOKEN="<JWT>"
```

前置：后端 `configs/config.yaml` 已配好公众号 appid/secret；服务器出网 IP 已加进公众号后台「IP 白名单」（否则微信报 45166）。
