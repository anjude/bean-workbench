# 260614 工作台初始化

## 目标

建立个人 Agent 工作台的基础结构，用于沉淀 skill、知识库、任务记录和常用脚本，并支持业务需求完成后的反向优化闭环。

## 范围

- 根目录入口文档。
- `docs/` 工作台规范。
- `.agents/skills/` 工作台 skill。
- `knowledge-base/` 知识库骨架。
- `task/` 任务登记。
- `script/` 和 `Makefile` 辅助入口。

## 边界

不改业务仓内容，除非后续出现明确业务需求。

## 当前状态

进行中。
