# 任务交接 handoff skill · 交接

- 任务目录：`task/260914_wb-handoff-skill/`
- 时间：2026-09-14 01:01
- 状态：进行中
- 上一份：无

## 这次到哪了

skill 本体、模板、编号登记、路由与检查脚本都接完了，还没跑 `make check`。

## 已完成

- 建 skill `wb-handoff`（SKILL.md + `references/handoff-template.md`）—— `.agents/skills/wb-handoff/`
- 编号登记 —— `AGENTS.md` 的 wb 表 + 阶段说明
- 路由接线 —— `.agents/skills/wb-router/SKILL.md` 关键词行、`task/registry.md` 热任务与路由提示
- 检查脚本登记 —— `script/check-workbench.py` 的 `REQUIRED_FILES`
- 方案文档 —— `task/260914_wb-handoff-skill/README.md`

## 未完成 / 下一步

1. 跑 `make check`（用 `/Users/bean/.workbuddy/binaries/python/versions/3.13.12/bin/python3 script/check-workbench.py`，助手 Bash 的 PATH 不含 homebrew）。判断标准：三项全通过，无新增问题。
2. 给老任务目录补 `handoff/`（可选，用户没要求就不动）。判断标准：用户点头后再建。
3. 实际用一次：下次会话中途说「handoff」，验证模板八段能不能填顺。判断标准：接手侧不看对话也能开工。

## 关键决策与原因

- wb 族整体去编号（豆哥定）：`wb-0001-router`→`wb-router`、`wb-0101-skill-refactor`→`wb-skill-refactor`、`wb-0102-handoff`→`wb-handoff`。wb 不跑流水线、没有阶段可排，编号只是记忆负担。`check-workbench.py` 里 wb 走独立正则，`wb-0102-handoff` 这类带编号的会被判不合规。
- 曾考虑 `wb-tools-handoff`：handoff 是用户显式触发的任务维护动作，不被流水线阶段调用，不符合 `*-tools-*` 定义，已否。
- 存任务目录而非系统临时目录：参考版存临时目录是为了隔离，工作台要的是跟着任务走、能 `git` 带走。
- 文件名带时间戳而非序号：按名排序天然得到时序，不用维护索引文件。

## 卡点与待确认

- 老任务目录要不要补 `handoff/`：等用户一句话。

## 环境与坑

- 无。

## 关联产物

| 产物 | 路径 |
| --- | --- |
| 方案文档 | `task/260914_wb-handoff-skill/README.md` |
| skill 本体 | `.agents/skills/wb-handoff/SKILL.md` |
| 模板 | `.agents/skills/wb-handoff/references/handoff-template.md` |
| 参考来源 | https://github.com/mattpocock/skills/tree/main/skills/productivity/handoff |

## 下次建议调用的 skill

- `wb-router`：建上下文、确认归属。
- `wb-handoff`：继续写交接或改模板。
- `wb-skill-refactor`：如果编号或归属要重排。
