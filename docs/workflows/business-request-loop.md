# 业务需求闭环

## 目标

让业务仓需求从识别、路由、执行到沉淀形成稳定闭环。

## 流程

1. 读取 `AGENTS.md` 和 `task/registry.md`。
2. 使用 `workbench-router` 判断业务仓和上下文。
3. 进入业务仓后读取子仓自己的入口说明。
4. 完成代码、文档和必要验证。
5. 回到工作台判断是否需要更新任务、知识库、skill 或脚本。

## 边界

业务仓实现细节留在业务仓，工作台只沉淀可复用的路由、约束、经验和问题。
