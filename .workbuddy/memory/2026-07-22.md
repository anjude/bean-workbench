# 2026-07-22 工作日志

## beannote：股市稳定挣钱主题 方向收敛
- 用户否决"叙事/口号式"框架，要求最科学、最直白、可操作：给公式+阈值+自测清单，让读者能自己算自己判，不要泛泛说教。
- 收敛出"科学版可操作"框架：① 备数据（净值序列）② 算基础四数（年化收益/波动/最大回撤/下行波动）③ 风险调整指标（夏普/索提诺/卡玛+阈值）④ 统计显著性（t≈夏普×√年数，需 (2/夏普)² 年证非零）⑤ 拆 α/β 回归 ⑥ 尾部风险一票否决（单笔亏>20%、杠杆/裸卖/满仓）⑦ 分段持续性检验。
- 2026-07-22 01:09 用户进一步收敛：不要过度公式化（去掉 t≈夏普×√年数 这类算式堆砌），重点放在"介绍测量方法 + 背后的道理 + 不夸大（老实说指标能证伪不能证明）"，平实叙述。即：科学但不端着，方法讲清楚、道理讲明白、局限说透。

## beannote 内容（豆小匠）
- 选题池在 `business-repo/beannote/02_topic_pool/`，每篇按「选题卡」格式（模板与阶段调性见用户级记忆 + content-beannote-creation-flow skill）。
- 投资认知内容方向（2026-07-22 用户强调整）：反对口号/说教式，要科学、平实、可操作。重点讲清"测量方法 + 背后的道理"，必要处给阈值但**不要公式堆砌**；老实交代指标局限（能证伪不能证明），不夸大。不要泛泛而谈的"劝人"文。
- 进行中主题「什么情况下我们认为一个人在股市能稳定挣钱」：已收敛出可操作科学版（备净值数据 → 基础四数 → 夏普/索提诺/卡玛+阈值 → 统计显著性 t≈夏普×√年数 → α/β 回归 → 尾部风险一票否决 → 分段持续性）。待用户确认是否落选题卡。

## 写作调性定稿（峰哥语气 + 仅剔除专属经历 + 保留加粗标重点）
- 用户最终澄清：峰哥的语气与风格（第一人称、口语、直白、辩证反转、自嘲、平视、真实>意义、标志性切入/收尾）全部保留；只需剔除峰哥"专属内容"——即个人经历与身份标签（三和/患癌保安/逃避劳动/缅甸乌克兰/蜂群文化/账号封禁等具体生平）。通用自嘲口吻（如"看热闹的""没收你钱"）可留，不属专属经历。
- 内容主题仍是投资认知（豆小匠选题池），科学骨架保留（5维度+局限），不夸大。
- 中间误把语气也改成豆小匠规整风被否；当前方向即：峰哥语气全留 + 去专属经历。
- 加粗标重点：用户明确"我要标重点"，故保留加粗小标题（第3版含加粗即定稿）。注：用户级记忆有 beannote 正文"加粗≤3处"约束，但本次用户明确要标重点，以最新指令为准，按维度标题加粗。落选题卡时若需收回≤3可再调。
- 冲突处理：playbook/创作准则明令"禁用你字、禁用破折号、正文限第三人称"，与峰哥语气（含你、——）冲突。已在选题卡"全篇写法定调"固化本篇特殊授权：以用户最新指令为准，生产时不再套用该通用约束。
- 已落选题卡：`business-repo/beannote/02_topic_pool/TODO_2026-07-22_什么情况下我们认为一个人在股市能稳定挣钱.md`（选题卡阶段，结论中性；态度版最狠结论留核心内容摘要阶段）。
- 标题定稿：用户拍板直接用疑问句「什么情况下我们认为一个人在股市能稳定挣钱」作主标题（原三个草案中"挣了钱≠你会炒股"降为小红书变体，"怎么判断…"为备选）。理由：读者现在不吃反常识钩子那套，要平实问题式。我曾提示"我们认为"与峰哥第一人称 voice 错位，用户以"就按我给的写"驳回，以最新指令为准。
- 已进入「核心内容摘要」确认门：新文件 `2026-07-22_..._核心内容摘要.md`（强判断模式，结论最狠有态度，守住平实科学不夸大）。五维度骨架对齐选题卡，锚点改用"熟悉的人"替代具体标的以守不点名边界。待用户确认后落正文。
- 正文已落：`01_in_production/2026-07-22_什么情况下我们认为一个人在股市能稳定挣钱/公众号文章.md` + `发布检查清单.md`（复盘留待发布后）。峰哥语气全篇（去掉模板默认"豆哥的笔记"开场白，直接峰哥切入）；6 处加粗标重点（用户授权，超出通用≤3约束）；不点名、留免责；结尾"这不就完了吗"轻收。

## backend-superone：公众号草稿发布接口（已落地）
- 需求：参考 `0731coderlee-sudo/wechat-publisher`（Node CLI），在 backend-superone 加一组调微信官方 API 把文章推**草稿箱**的接口。复用现有 `third_party/wechat` 微信集成层（token 缓存+重试），新增草稿/永久素材能力。
- 凭证交给用户后落地：appid=wxf20fd10225f69701，secret 已填；仅进 `configs/config.yaml` 的 `wechat_mp_config`，不在 Go 硬编码；`init_config.go` 的 `reloadConfig` 在 `Unmarshal` 后把凭证注入 `constant.AppIdSecret`，token 机制零改动（用户拍板：请求不带 app_id，后端用配置凭证，与调用方 JWT 的小程序 appid 解耦）。
- 落地文件：① `third_party/wechat/constant.go` 增 `draftAdd`/`materialAdd` 端点；② `schema.go` 增 `DraftArticle/DraftAddReq/DraftAddResp/MaterialAddResp`；③ `wechat.go` 的 `IWechatApi` 增 `AddDraft`/`AddMaterialImage` 并 `Api` 实现（套 `doWithAccessTokenRetry`）；④ `config/define.go` 增 `WechatMpConfig`+`Config.WechatMp`；⑤ `init_config.go` 注入 `AppIdSecret`；⑥ `config.yaml` 填真实凭证；⑦ `common_dto.go` 增 `CreateWechatDraftReq/Resp`、`UploadWechatMaterialResp`；⑧ `common_service.go` 增 `CreateWechatDraft`/`UploadWechatMaterial`+辅助 `markdownToHTML`(goldmark)/`downloadToTemp`/`replaceExternalImages`(正则扫外链 img)/`isWechatImgHost`；⑨ `common_use_case.go` 增两方法；⑩ `api_service.go` 注册 `POST /api/so/wechat/article/draft`、`/wechat/material/image`（均走 `middleware.HandleRequest`+JWT）；⑪ `frontend-contracts/openapi/wechat_api.yaml` 新建契约；⑫ `go.mod` 增 `github.com/yuin/goldmark v1.7.8`（go.sum 已补）。
- 验证：`gofmt`/`go vet`/`go build ./...` 全通过。`wechat_api.yaml` 结构校验（components 在第 0 列）通过。
- 待用户做：① 把服务器出网 IP `8.155.38.83` 加入公众号后台「IP 白名单」（否则微信报 45166）② 以含凭证的配置启动服务后做端到端建草稿验证（本期未执行真实调用）。
- 设计文档：`backend-superone/docs/feature/2026-07-22_公众号草稿发布方案.md`（含功能/回归/兼容性三表 + 落地记录）。

## skill：把草稿接口能力接入 content-wechat-article-flow
- 用户拍板：能力加到 **content-wechat-article-flow**（用户级 `~/.workbuddy/skills/`，非 content-creation）；鉴权用「我给 token+地址」方案（写进 skill 本地配置，不进 git）。
- 改动：① SKILL.md frontmatter 描述补「调后端接口一键发布到公众号草稿箱」；② 新增「step 4. 发布到公众号草稿箱」环节——含 `POST {BASE_URL}/api/so/wechat/article/draft` 的字段映射表（title/content|content_markdown/author/digest/content_source_url/cover_url|thumb_media_id/auto_upload_images/评论开关）、curl 模板、`Authorization: Bearer` 鉴权说明、只进草稿箱不群发的提醒。③ 新建 `references/公众号草稿发布-本地配置.md`：放 `BASE_URL`+`TOKEN`（占位），含验收 curl 与前置检查（IP 白名单 / yaml 凭证 / token 过期）。
- 注：`content-creation`（beannote，经 `.lnk` 指向 superone/beannote）是更全的内容创作 skill，本期未改；如后续要在那边也接发文，可复用同一份本地配置与 curl。

## backend-superone：生成永不过期 JWT token
- 用户要一个永不过期的调用 token。JWT 用 HS256，密钥 = `global.Conf.GetJwtKey()` = `config.App.JwtKey`。各档密钥：config.yaml=`ashdfkashdnfkjash`（= 脚本 `defaultJwtKey`，且公众号凭证只加在 config.yaml，故运行期必走此档）、config.dev/test.yaml=`astest`。
- 现成 `scripts/gen_jwt_token.go` 原固定 30 天过期、硬编码 `defaultJwtKey`。已改进：① 加 `-expire=0` → 不设 `ExpiresAt`（golang-jwt v4 在 exp 缺省时不校验过期，即永不过期）；② 加 `-key` 可覆盖密钥以匹配其他运行档。
- 已生成：`go run scripts/gen_jwt_token.go -expire=0`，payload 校验无 `exp` 字段（确认永不过期），claims=Openid/Admin:0/AppId:wx7752b4b866490bdd/LoginType:1。token 已写入 `content-wechat-article-flow/references/公众号草稿发布-本地配置.md` 的 TOKEN（BASE_URL 仍占位待用户给）。
- 风险点：token 仅当服务器以 `config.yaml` 档（jwt_key=ashdfkashdnfkjash）运行才有效；若跑 dev/test/livetest 档需 `-key=astest` 重新生成。
