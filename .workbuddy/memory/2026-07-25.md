# 2026-07-25

## carbon 边界模块新增类目（设计讨论）
- 用户拍板：新类目"我的承诺/习惯"作为**边界（个人使用说明书）下的第 4 个并列类目**，与底线/喜欢/不喜欢平级（非独立模块）。
- 名字三候选仍在：默认设置（平级结构下推荐）/ 我会做 / 我记着的。用户尚未最终拍名字。
- 关键差异：新类目条目级带 `是否对对方展示`（默认展示）；其余三栏始终公开，UI 不暴露开关。
- 数据模型：carbon_boundary_tab 加 `category` 枚举值（如 commitment）+ `item_type`(promise/habit，仅本类目) + `visible_to_partner`(默认1)。list 查对方时过滤 visible=0。
- 待用户拍：①最终命名 ②本类目对方表态是否沿用"懂了/还需磨合"还是换"收到/一起"类。
- 未动代码（讨论阶段）。若落地，按 dev-flow 写单篇 feature 文档到 business-repo/uni-carbon-space/docs/feature/，并增量 append carbon_api.yaml。

## 2026-07-25 深夜：边界「我会做」类目已全链路落地
- 用户拍板：第 4 个并列类目命名「我会做」，category=4；对方表态文案「收到 / 一起」（底线/喜欢/不喜欢沿用「懂了/还需磨合」）。
- 数据模型：carbon_boundary_tab 新增 item_type(0无/1承诺/2习惯，仅我会做用) + visible_to_partner(默认1，仅我会做可设0仅自己可见)。repo 层 ListBoundaries 加 viewerOpenid 参数做隐私过滤 `openid=? OR (openid<>? AND visible_to_partner=1)`。
- 后端：constant 加 CarbonBoundaryCategoryCommitment=4、BoundaryReactionText(category感知)、CarbonBoundaryItemType；service 层对我会做校验 item_type∈{1,2}、默认 visible=1、改类目时重置 item_type=0；react 时对仅自己可见条目拒绝表态。
- 前端：types/carbon.ts + types/api/carbon.ts 加 itemType/visibleToPartner 字段与映射；useBoundary 的 create/update 支持 opts；index.vue 新增「我会做」定制底部表单（内容+承诺/习惯分段+对Ta展示开关），其余三栏走原 modal。struggleCount 与磨合高亮排除我会做。
- 协议：carbon_api.yaml 增量——BoundaryView/Create/Update/List 的 category enum 加 4，Create/Update 加 item_type/visible_to_partner，react 描述补全我会做文案与隐私拒绝。
- 验证：后端 `go build ./...` 通过；`go vet` 仅 main.go 信号通道告警（预存，无关本次）；前端 `vue-tsc --noEmit` 通过（修了两处漏 import 的 BoundaryItemType 与 switch change 事件类型）。DDL 已授权执行于共享测试库（8.155.38.83），ALTER 成功（非新表，无需改 carbonTables）。
- 易踩坑已修复：①ICarbonRepo 接口 ListBoundaries 签名未同步 → 编译报错，已补 viewerOpenid；②前端 BoundaryItemType 在 useBoundary.ts 与 index.vue 两处漏 import。
- DDL 两个环境均已执行：先 `-env=test`（weiyi_superone_db_test），用户追问后授权 `-env=live`（weiyi_superone_db，生产库），两次 ALTER 均成功。两库 host 同为 8.155.38.83，靠库名区分。
- 后端已提交并推送：commit `212d96b` on `dev` → `origin/dev`，7 文件 +110/-16，含 DDL 迁移脚本。message: `feat(carbon): 边界新增「我会做」类目`。仅后端仓，前端/yaml/doc 另仓未动。

## 边界「我会做」上线后三处 bug 修复（用户联调反馈）
- bug1 私密没生效：后端 `carbon_repo.go` 的 `UpdateBoundary` 原用 GORM `Updates(tab)` 结构体，会跳过零值字段，导致 `visible_to_partner=0`（仅自己可见）落不了库。改为 `Select("content","category","item_type","visible_to_partner").Updates(tab)`，强制写入选中列含零值。前端已正确把 bool 转 0/1 发送（useBoundary 68/92 行）。
- bug2 弹窗无两套主题：`.boundary-form` 是 `.boundary-shell` 兄弟节点，原没挂主题类、sheet 用硬编码深色渐变。改为 form 根挂 `:class="themeClass"`，sheet 改用 `--cs-frost/--cs-border/--cs-shadow-deep/--cs-text/--cs-text-secondary/--cs-glow-light`，均已在 variables.css 的 theme-day/theme-night 定义。
- bug3 保存按钮没居中：用户澄清是**上下（垂直）没居中**，非左右。微信原生 `<button>` 不认 `display:flex`，改用 `line-height:88rpx`（=height）+ `text-align:center` 垂直+水平居中才生效。
- 验证：`go build ./...` 通过；`vue-tsc --noEmit` 通过。三仓库全部提交推送完毕：
  - backend-superone (dev)：`212d96b` 主功能 + `e72d116` 私密修复（仅 carbon_repo.go）。
  - uni-carbon-space (master)：`7d459b2` 前端全量（types/composable/index.vue + feature 文档，5 files +574/-13）。
  - frontend-contracts (master)：`054ec3c` 仅 carbon_api.yaml（1 file +37/-10）。注意：该仓另有一个无关的未跟踪文件 openapi/wechat_api.yaml，未纳入本次提交，留待用户自行处理。

## 边界「我会做」前端补删除 + 文案复盘（用户二次反馈）
- 删除入口缺口：后端 `/carbon/boundary/delete` 接口一直存在（yaml line 542 / 路由 line 160 / service line 1006 / 前端 api line 269），但前端 `onItemTap` 对 commitment 类目直接进编辑表单并 return，且编辑表单原本无删除按钮——所以我会做条目从 UI 删不掉。已在 `index.vue` 编辑表单（仅 edit 模式）底部加「移除这条」文字按钮，复用 `deleteBoundary(commitFormId)`；新增 `deleteCommitItem()`，成功后关表单。`vue-tsc` 通过。改动未提交。
- 文案复盘（用户：我会做文案太暧昧、前期建空间有压力，问其他文案是否同类问题）：
  - 根因：4 类目里只有「我会做」是**向对方做承诺**，其余三栏（底线/喜欢/不喜欢）都是陈述自己偏好，天然低压。我会做的文案把这种承诺感放大了。
  - 最重：`我会做` 的对方表态「一起」= 把个人承诺变成共同承诺，前期压力最大；其次 emptyHint「想清楚自己会怎么对这段关系，写下来。」像合同 homework；类目名「我会做」本身已带承诺感。
  - 入口卡 desc「怎么和我相处，一份使用说明书」轻度暧昧：设备隐喻可爱但模糊了真实用途（一组期望/边界），且把对方放成「操作我的人」。
  - 干净无问题：底线「红线，你自己来画。」、喜欢「喜欢被这样对待。」、不喜欢「别这样对我。」、反应「懂了/还需磨合」、删除弹窗文案。
  - 建议软化方向（待用户拍）：反应 一起→陪你/想一起；emptyHint 由指令式改为自我邀约式；类目名「我会做」→「我愿意/我的方式」；入口卡 desc→「关于我，一些希望你先知道的事」。另：删除弹窗 confirmColor `#c85a5a` 偏红，违反 carbon「无红/绿独立色」硬约束（旧三栏删除弹窗也如此，属预存偏差）。

## 内容生产 skill 锐评（用户要求"用峰哥skill锐评"）
- 修正：首轮在 ~/.workbuddy/skills 与项目目录搜"峰哥"未果；用户指出"在用户目录的.agents下"后确认 skill 真实存在：`~/.agents/skills/feng-ge-perspective`（周丽峰/峰哥亡命天涯，街头社会学家视角，非默认 skill 列表成员）。
- 按 峰哥 第一人称视角重做锐评（对话交付，未落文件），套用其模型：辩证反转/赢学、真实>意义(别强行上意义)、底层是认知位置非地理位置、平视不俯视、程序员转行自嘲、灵魂拷问。主动避开其"性压抑大一统理论"（评写作 skill 硬套两性理论属强行上意义，不当用）。
- 工作台内容生产 skill 实际就两份：①`content-creation`（用户级 symlink→beannote 仓，大而全：投资理财/公众号/小红书/选题/正文/发布/复盘+3份reference）②`content-wechat-article-flow`（用户级，公众号选题+写作+一键推草稿箱）。
- 锐评核心发现（结构性问题，未来若要修可参考）：
  1. 两份 skill 在"公众号写作"职责重叠，无清晰调用边界，用户不知调谁。
  2. 写法规则直接矛盾：`content-creation` 禁第二人称"你"/破折号/角引号；`content-wechat-article-flow` 标题范例却用"你"字当头，结尾一个要克制一个要互动钩子。
  3. `content-creation` 前置重（读5份文件）、产出重（至少4文件），随手发场景被仪式感劝退。
  4. "自进化"机制单条反馈可能改全局规则，缺闸门，有规则漂移/臃肿风险。
  5. 质检清单全为主观软指标，AI 自报"通过"，存在信任黑洞。
- 亮点也认可：选题排雷（识别烂大街模板）、去平庸标准、一键推草稿箱超能力。
- 用户尚未要求落地修改；若后续说"合并/统一内容skill"，按锐评第1、2条先划边界、统一写法铁律。

## 边界文案纠正：听岔"我也有点难搞"（重要，避免再错）
- 用户说"我也有点难搞那个文案"指的是**空状态引导标题**（index.vue line 79：`boundary-shell__invite-title`），原文`我也有点难搞，先写几条给你`——是自嘲式空状态邀请语，用户觉得暧昧/前期建空间有压力。
- 我一开始误当成「我会做」对方表态的「一起」，把三处「一起」→「陪你」全改了，被用户纠正。已**全部撤回**（constant/carbon.go、index.vue reactionLabel、carbon_api.yaml react 描述恢复「一起」）。
- 最终只改 line 79 标题：「我也有点难搞，先写几条给你」→「先写几条，让对方知道怎么对你好」（去自嘲、不施压、呼应下面 desc 但不重复"懂你"）。表态「一起」保持原样，其余文案不动。
- 教训：用户说"某某文案"时优先去代码里搜该字面字符串定位，别按自己上轮的复盘分类去猜。
- 边界空状态标题(line 79)文案禁忌（连续踩雷，牢记）：①"我也有点难搞，先写几条给你"=自嘲式，用户嫌暧昧/前期施压；②"先写几条，让对方知道怎么对你好"=用户判**登味**（居高临下、像表演对对方好，越界）。终版="先写几条，从最在意的开始"——只讲用户自己从哪动笔，不绑架对方、不抬举自己。碳基文案铁律：写自己的需求，别当关系导师，别发好人卡。

## 边界文案终版提交（用户：不改了就这样，各仓库提交推送）
- 误改「一起」→「陪你」三处已全部撤回，后端(constant/carbon.go)与协议(carbon_api.yaml) git 状态干净，无需再推。
- 实际未提交改动只在 uni-carbon-space（index.vue）：①我会做删除入口 ②空状态标题终版"先写几条，从最在意的开始"。
- 已提交推送：commit `ebd7205` on master → `origin/master`（`7d459b2..ebd7205`，1 file +30/-1）。message: `feat(carbon): 边界「我会做」补删除入口 + 空状态标题去登味`。
- frontend-contracts 仓仍有无关未跟踪文件 `openapi/wechat_api.yaml`，未纳入、未动。
