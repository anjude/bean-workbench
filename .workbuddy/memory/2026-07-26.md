# 2026-07-26

## 内容生产 skill 重构（用户拍板并确认执行）
背景：用户用 feng-ge-perspective（峰哥）视角锐评后，决定简化整个内容生产系统。

落地改动（均在 beannote 仓 `business-repo/.../beannote/.workbuddy/skills/content-creation`，用户级 symlink `~/.workbuddy/skills/content-creation.lnk`）：
- **收敛成一个 skill**：保留 `content-creation`；删除 `~/.workbuddy/skills/content-wechat-article-flow`（用户选"删除，发布能力并入"）。其"一键推草稿箱"整段 + 本地配置 reference `公众号草稿发布-本地配置.md` 已并入 content-creation（含原 JWT/BaseURL，未改动密钥）。
- **简化落盘**：去掉 `01_in_production` 状态机；每篇独立文件夹 `YYYY-MM-DD_主题简述` 自闭环。维持三文件（公众号文章.md + 发布检查清单.md + 复盘.md），**复盘.md 仅在有用户反馈或规则沉淀时写**，无则省略。
- **创作准则新增"第一准则：平视（把读者当人）"**：不俯视（不纠偏/不教做人）、不仰视（不讨好/不堆噱头骗点击）、不脸谱化。派生真实>意义、信息量、去AI味。
- **标题去标题党**：原"爆款意识/标题让人愣一下"改为"标题=真实判断或真实问题"，禁夸张恐吓/制造焦虑/数字shock无实质；给 before/after 示例。
- **去AI味升为硬原则**：从"可谨慎使用"改为直接禁用，扩清单（"在这个X时代""毋庸置疑""首先其次最后"模板"它不仅更"定义式开头滥用/升华段尾/主持人预告句/解释笑点）。
- **正文质检**：标题检查改为平视口径；每条"通过"需附原文行号或片段（硬证据），不空打勾。
- **自进化机制：暂不动**（用户明确）。
- 模板合集：默认文件组合加自闭环说明；复盘.md 状态字段改为可选记录。

注意（已更正）：**昨天改错了副本。** 用户说的"工作台的 content 创作的 skill"实际是 `E:\workspace\bean-workbench\.agents\skills\content-beannote-creation-flow\SKILL.md`（workspace 级、真正在用、更完整：含 认知深度方法论.md / 生产前确认门 强判断·事实流 / 接 beannote playbook）。而昨天我误改的是 beannote 仓的 `content-creation`（符号链接进 `~/.agents/skills/` 与 `~/.workbuddy/skills/`，较老副本）。所以 A(content-beannote-creation-flow) 仍是旧规则、未享受任何改动。两份非简单复制，A 应作为唯一真身，B 是冗余旧副本。

待办：把昨天那套改动重新应用到 A、并入公众号发布能力、退役 B（删 beannote content-creation + 两个 symlink）。自进化仍暂不动。改动均未 git 提交。

## 已收尾（用户说"要的"后执行）
改动已重做进 **A = `E:\workspace\bean-workbench\.agents\skills\content-beannote-creation-flow`**，并保留 A 多出的认知深度方法论 / 生产前确认门 / beannote playbook 接线：
- 创作准则.md：新增「第一准则：平视（把读者当人）」置顶；原「爆款意识」改写为「标题（平视，不标题党）」；新增「去AI味（硬原则，不是可谨慎）」禁用清单；「允许但要有分寸」移除去重（定义式开头/升华段尾已列入硬禁）。
- 正文质检清单.md：爆款检查标题项改平视口径；文风检查新增「无AI套话」「每条通过附原文行号/片段硬证据」；文件检查与自进化检查中 复盘.md 改为可选。
- 模板合集.md：默认组合加「每篇自闭环、不再分待生产/已发布/归档」；复盘.md 状态字段改可选。
- SKILL.md：触发步骤去掉 01_in_production/README 门禁并重排为四步（含平视对照、硬证据）；默认落盘去 01_in_production；文件策略表/文件检查/复盘段 复盘.md 改可选；新增「发布到公众号草稿箱（调 backend-superone 接口）」段落；参考资源表加本地配置行。
- 新增 `references/公众号草稿发布-本地配置.md`（含 JWT，本地不进 git）。

**退役 B**：已删除 beannote 仓 `content-creation` 目录 + 用户级两个符号链接（`~/.agents/skills/content-creation`、`~/.workbuddy/skills/content-creation.lnk`）。现 bean-workbench `.agents/skills/` 下内容创作仅剩 `content-beannote-creation-flow`（另两 content-* 为 article-review、knowledge-base，职能不同）。**A 即唯一内容创作 skill。**

未 git 提交（A 在 bean-workbench 仓；beannote 的 B 已物理删除，无遗留未提交改动）。自进化机制按用户要求暂不动。
