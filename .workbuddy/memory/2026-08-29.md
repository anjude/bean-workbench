# 2026-08-29

## 工作台 skill 全量编号迁移

用户要求给 `.agents/skills/` 下两套（实际四套）skill 族群按开发顺序编号，格式 `{族群前缀}-{四位序号}-{能力后缀}`。

决策（用户拍板）：全部编号（不只 be/fe），命名去掉族群前缀/仓名前缀。
- `be-*` 后端开发流水线 8 个：dev-flow(0000) → domain-flow → field-sql-flow → db-change-flow → api-flow → wire-flow → contract-flow → test-review-flow(0007)
- `fe-*` 前端开发流水线 11 个：dev-flow(0000) → product-archetype-flow → state-matrix-flow → space-ui(原 carbon-space-ui) → api-flow → state-flow → page-flow → component-style-flow → svg-flow → icon-flow(原 carbon-icon-flow) → test-build-flow(0010)
- `cn-*` 内容生产线 3 个：knowledge-base(0000) → creation-flow → article-review
- `wb-*` 工作台基建 2 个：hair(0000) → router(0001，原 workbench-router)

执行方式：Python 脚本按旧名长度降序做全仓文本替换（长名优先避免误命中），再 `git mv` 重命名目录。共改 36 个文件。

同步范围：`SKILL.md` 的 `name` 与正文交叉引用、`AGENTS.md`（新增「当前 skill 编号表」+ 重写命名规则段）、`docs/workspace.md`（命名格式段）、`docs/workflows/business-generation-checklist.md`（拆开 `uni-*` 通配为逐条编号）、`task/registry.md`、`task/260628_skill-naming-normalization/README.md`（补全量迁移清单）、`task/260614_uni-carbon-space-onboarding/README.md`、`script/check-workbench.ps1`、`.workbuddy/memory/MEMORY.md`。

未改动（按只读处理）：`business-repo/` 业务子仓（含 `uni-carbon-space/.agent/skill/carbon-space-ui/` 私有副本与 feature 设计文档）、`.workbuddy/memory/2026-*.md` 历史日志。

校验：24 个目录名与 `SKILL.md` 的 `name` 全一致；工作台范围内旧名残留 grep 为空；check-workbench.ps1 路径清单手动校验全 OK（本机无 pwsh）。

遗留提醒：业务子仓 `business-repo/` 内仍留有旧 skill 名（如 carbon-space-ui），下次进子仓时如需一致再单独处理。

## 第二轮：编号改阶段式 + 内容线前缀改 bn

用户两条反馈：① 四位编号要拆两段，前两位=阶段号、后两位=阶段内序号（设计阶段 0001/0002，开发阶段 0101/0102）；② beannote 的 skill 前缀应为 `bn` 而非 `cn`。

阶段划分（be/fe 共用，便于跨端对齐）：`00` 设计（总控 + 前置设计）、`01` 开发（编码实现）、`02` 交付（契约/构建/测试/审查）。`bn` 为 `00` 素材 / `01` 创作 / `02` 复盘。`wb` 为 `00` 入口 / `01` 路由。阶段内 `xx00` 留给总控/入口，具体 skill 从 `xx01` 起。

重排结果（仅这些变了，其余保持）：
- `be-0003-db-change-flow`→`be-0101`、`be-0004-api-flow`→`be-0102`、`be-0005-wire-flow`→`be-0103`、`be-0006-contract-flow`→`be-0201`、`be-0007-test-review-flow`→`be-0202`
- `fe-0004-api-flow`→`fe-0101`、`fe-0005-state-flow`→`fe-0102`、`fe-0006-page-flow`→`fe-0103`、`fe-0007-component-style-flow`→`fe-0104`、`fe-0008-svg-flow`→`fe-0105`、`fe-0009-icon-flow`→`fe-0106`、`fe-0010-test-build-flow`→`fe-0201`
- `cn-0000/0001/0002`→`bn-0001-knowledge-base`、`bn-0101-creation-flow`、`bn-0201-article-review`
- `wb-0001-router`→`wb-0101-router`

第二轮同样用 Python 脚本替换（32 个文件）+ `git mv` 16 个目录。脚本刻意排除了 `MEMORY.md`、当日日志、`task/260628` 三处历史映射记录，避免两代编号被机械替换搅在一起，之后手工重写。

同步改写的规则文档：`AGENTS.md`（命名约束加阶段语义 + 编号表按族群分四张表并标注阶段）、`docs/workspace.md`（命名格式与原则改为两段编号）、`business-generation-checklist.md`（skill 组合按 00/01/02 三阶段重排）、`task/registry.md`（`cn-*` 改 `bn-*`）。

校验：24 个目录名与 `name` 再次全一致；`grep -E "cn-00|be-000[3-7]|fe-000[4-9]|fe-0010|wb-0001"` 在工作台范围内为空。

## 第三轮：be 族按四阶段重构 + 建工具层

用户判断前两轮只是把旧名换成编号，流水线语义还是平的，要求按「方案设计 → 后端开发 → 前端开发 → 整体验证」重构，并加 `be-tools-xxxx` 工具 skill（不属于任何阶段）。本次不动 `fe-*`，但把阶段位留出来给将来合族。

用户拍板三个决策：① 阶段号从 `00` 起，拆掉 `be-0000-dev-flow` 总控（196 行），方案设计独立成 `be-0000-plan-flow` 兼做链路入口；② 方案产出落工作台 `task/YYMMDD_{主题}/README.md`，不再往业务仓 `docs/feature/` 新增（后端仓该目录有历史文件 `2026-07-22_公众号草稿发布方案.md`，其结构含落地记录章节，被吸收进新模板）；③ 工具层首批建 project-context / db-query / branch-release（不要 code-search）。

最终结构 11 个：
- 00 方案设计：`be-0000-plan-flow`
- 01 后端开发：`be-0101-domain-flow`、`be-0102-field-sql-flow`、`be-0103-db-change-flow`、`be-0104-api-flow`、`be-0105-wire-flow`、`be-0106-contract-flow`
- 02 前端开发：位保留给 `fe-*`
- 03 整体验证：`be-0301-test-review-flow`
- 工具层：`be-tools-project-context`、`be-tools-db-query`、`be-tools-branch-release`

踩坑与经验：
- **编号循环占用**：`be-0001-domain-flow`→`be-0101` 时 `be-0101` 已被 db-change 占着，而 db-change 要挪到 `0103`、`0103` 又被 wire 占着。必须按拓扑序先移走占位者（wire→0105，db-change→0103，api→0104，再 domain→0101、field-sql→0102）。目录重命名用 `mv` 排队解决；文本替换无此问题。
- **脚本会误伤历史叙述**：全仓替换脚本把设计文档「背景」里描述旧总控的 `be-0000-dev-flow` 也换成了新名，语义错乱。历史映射记录（MEMORY.md、`task/260628`、新设计文档的背景段）必须预先排除、事后手工重写。
- **内容不能只换名**：查了后端仓 Makefile / push.bat / scripts/migration 才写准工具 skill——`push.bat` 在临时 worktree 里合并、本地未提交改动不带进去；`make test` 是 `go test -short ./...`，集成测试需 `RUN_INTEGRATION_TESTS=1`；migration action 只有 clear/migrate/import-address/import-spot/exec-sql/verify-carbon，**没有只读查询 action**，所以 `be-tools-db-query` 走 mysql 客户端直连，凭据读 config.go。
- 老的「自包含、不依赖知识库目录」开头统一改成指向 `be-tools-project-context`，消除各 skill 重复抄路径约定。

职责边界（写进设计文档防越界）：field-sql 只设计不连库 / db-change 才执行写操作；db-query 严格只读；contract-flow 只生成契约、消费属阶段 02。

校验：27 个目录名与 `name` 全一致；悬空引用检查脚本仅报出三处刻意保留的历史映射文档；`check-workbench.ps1` 的 17 项路径手动校验全 OK（本机无 pwsh，该脚本只校验 wb-* 两个 skill，不涉及 be-*）。

设计文档：`task/260829_be-skill-refactor/README.md`。

## dev-flow 跨端合族（下午）

把 `be-*` 后端族（8 个）与 `fe-*` 前端族（11 个）合并为单一跨端族 `dev-flow-*`，一次需求走一条流水线。设计文档 `task/260829_dev-flow-merge/README.md`。

- 命名：`dev-flow-{阶段号}{阶段内序号}-{后缀}`，工具层 `dev-flow-tools-{后缀}`。四阶段：00 方案设计 / 01 后端开发 / 02 前端开发 / 03 整体验证，纯单端需求可跳过对应阶段。
- 编号映射：be-00xx→dev-flow-00xx、be-01xx→dev-flow-01xx 不变；fe 的 9 个开发 skill 对齐到 `dev-flow-0201~0209`；验证拆成 `dev-flow-0301-backend-verify-flow`（原 be-0301）与 `dev-flow-0302-frontend-verify-flow`（原 fe-0201，整体重写）。
- 拆掉 `fe-0000-dev-flow`（176 行前端总控）：项目上下文并入 `dev-flow-tools-project-context`（从后端单仓扩为四仓速查，写入前去前端仓核实了真实目录与脚本），开发原则分散进 0201/0202/0207/0208，约束进 0205/0206，工作流与输出格式进 plan-flow。
- plan-flow 扩展：前端需求解析、前端影响面落点、阶段 02 派发表、阶段 02 执行顺序、前端门禁项（页面状态分支）。
- 两端都有 `api-flow`（0104 后端接口 / 0204 前端类型），靠阶段号区分，不改后缀。
- 同步：AGENTS.md（命名规则 + 编号表重写）、docs/workspace.md、business-generation-checklist.md、task/registry.md、wb-0101-router（补方案入口路由）、wb-0102-skill-refactor（补合族踩坑）、长期记忆。
- 踩坑：写 tools-project-context 时不能直接抄旧总控里的路径，必须去业务仓核对（前端仓实际有 constant/utils 等旧文档没写的目录）；说明性写法（如 `dev-flow-01*`）会被悬空引用扫描误报，文档里改用「`dev-flow-0101` 起」这类写法。

校验：27 个 skill 目录名与 name 全一致；无悬空引用；plan-flow 派发表覆盖全部 18 个阶段 skill；工作台结构 17 项路径全在。

## dev-flow skill 合并精简（27 → 18）

用户反馈 skill 太多，做一轮合并精简。设计文档 `task/260829_dev-flow-slim/README.md`。

- 合并原则：同阶段内「总是一起走」且各自只有几十行的 skill 合并成一个，用编号章节区分职责；**合并优先于拆分**。
- 后端 6→4：`0102-field-sql` 并入 `0101-domain-flow`（领域层+字段与 SQL 设计，只设计不连库，references/field-design-norms.md 随迁）；`0105-wire` 并入新 `0103-api-flow`（接口+Wire 装配）；原 `0103-db-change`→`0102`；原 `0106-contract`→`0104`。
- 前端 9→5：`0202-state-matrix` 并入 `0201-archetype-flow`（原型+状态矩阵，硬门槛合一）；`0205-state` 并入 `0202-data-flow`（API 类型+Repo+store/composable）；`0207-component-style` 并入 `0203-page-flow`；`0209-icon` 并入 `0204-asset-flow`；`0203-space-ui`→`0205-space-ui`，从「执行第 3 步」改为**全程品牌叠层**（命中 carbon 时在页面/资产之前先读）。
- 验证 2→1：`0301-verify-flow`（按端分节，方案回填统一负责，不再分前后端）。
- 工具 3→2：`tools-project-context` + `tools-branch-release` → `tools-repo`。**`tools-db-query` 不动**：只读与写操作（0102-db-change）必须隔离，合并会让「只做读、禁止写」的约束自相矛盾。
- wb 3→2：`wb-0000-hair` + `wb-0101-router` → `wb-0001-router`；`wb-0102-skill-refactor`→`wb-0101-skill-refactor`。
- bn 3 个不动：creation-flow 已 511 行，再并 article-review 会过载；素材/创作/复盘三个阶段语义清晰。
- 顺手补进合并后的 skill：gin 数值 0 值校验坑（`binding:"gte=0"`）、carbon 表需手动加进 `exec_sql.go` 的 `carbonTables`、前端 `src/static/icons/` 目录条目。
- `wb-0101-skill-refactor` 补「5. 合并精简」步骤 + 合并判定原则（该并/不该并三类）+ 两条踩坑（悬空扫描要排除历史文档与对照链；批量 mv 先落 tmp-* 前缀）。
- 同步：AGENTS.md（编号表 + 新增合并粒度原则）、docs/workspace.md（示例 + 原则，原第 8 条顺延为 9）、business-generation-checklist.md、business-request-loop.md（漏扫，补 `wb-0001-router`）、docs/goals/README.md、task/registry.md、script/check-workbench.ps1、 MEMORY.md（编号段重写 + 合并对照链）。
- 校验：18 个 skill 目录名与 name 全一致；活跃文档无悬空引用（历史任务文档与对照链里的旧名属刻意保留）；plan-flow 派发表覆盖全部 13 个阶段 skill；结构校验无缺失；references 资产随迁完好。
