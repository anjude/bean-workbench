# 2026-09-08

## bn 族群能力梳理
- 通读 `bn-0001-knowledge-base`、`bn-0101-creation-flow`、`bn-0201-article-review` 三个 SKILL.md 及 references，核对 `business-repo/beannote/` 实际目录（01~08 + docs/content-playbook.md 均存在）。
- 发现待修项：`bn-0101` 参考资源表列了 `references/公众号草稿发布-本地配置.md`，但 references 目录下实际没有该文件；未见 gitignore、无 git 历史，属引用悬空。

## bn 族群 skill 与实际目录的三处冲突（复核确认）
1. `bn-0201` 规定复盘存 `08_archive/review_YYYYMMDD_*.md`，实际 `08_archive/` 是「废弃稿/过期选题/历史版本，不写复盘」的整篇目录，全目录无 review_ 文件；复盘实际写在每篇目录下的 `复盘.md`（由 bn-0101 生成）。
2. `bn-0101` 称「每篇自闭环，不再分待生产/已发布/归档」，实际仍是 `01_in_production` / `07_published` / `08_archive` 三段流转。
3. `references/公众号草稿发布-本地配置.md` 引用悬空（同上）。
另：创作准则「允许但要有分寸」小节注释写「上节去AI味硬禁令」，实际该节在文件末尾（下节）。

## beannote 目录重构（已执行，解决上面冲突 1、2）
- 8 个顶层目录 → 4 个：`topics/`（一篇一目录，唯一生产区）/ `knowledge-base/` / `data/` / `docs/`。
- 状态从目录层级下沉到目录名前缀：无=在制、`done-`=已发布、`drop-`=已归档；发完只在同层改名，不再跨 01→07→08 搬家。
- 每篇固定槽位：`01_选题卡` `02_脚本` `03_正文` `04_发布稿` `05_检查清单` `06_复盘` + `assets/`。旧名已批量改（公众号文章→04、纯享正文→03、图文脚本→02、发布检查清单→05、复盘→06）。
- 用 git mv 迁移 31 篇，全部识别为 rename，历史保留；生成 `topics/索引.md`（在制 20 / 已发布 4 / 归档 7）；旧 9 个 README 合并进 `README.md` + `docs/目录规范.md`。
- 同步改活跃文档：`docs/content-playbook.md` 落盘规则与 skill 名、`data/README.md` 字段表（对齐 CSV 实际表头）、`knowledge-base/README.md`、`.agents/skills/bn-0101-creation-flow/SKILL.md`（目录约定+槽位名）。历史复盘文件里的旧路径按约定保留。
- 待用户确认：疑似同题两版 `2026-07-20_越早理财越好但复利不是原因` vs `...不是洗脑`、`2026-05-20_分红有没有意义` vs `2026-05-21_股息分红有没有意义`；原 `01_in_production` 的 10 篇都有复盘，看着像已发，是否统一加 `done-`。
- 未提交，改动全在 beannote 工作区。

## bn 族群交互化重构（已执行，方案见 task/260908_bn-skill-refactor）
- 3 个 skill → 9 个：6 阶段（`bn-0000-topic-flow` 选题 / `bn-0101-discuss-flow` 讨论 / `bn-0102-draft-flow` 成稿 / `bn-0201-review-flow` 审稿 / `bn-0301-publish-flow` 发布 / `bn-0302-retro-flow` 复盘）+ 3 工具（`bn-tools-knowledge-base` / `bn-tools-content-context` / `bn-tools-writing-rules`）。
- 524 行总控拆开后单文件 81-124 行（tools-knowledge-base 193 行未拆）；每个阶段 skill 头部补「用户说什么 / agent 做什么 / 产物 / 停下等什么」+ 上下游指向。
- references 归位：创作准则→tools-writing-rules、正文质检清单→0201、认知深度方法论→0101、模板合集拆成选题卡/核心内容摘要/成稿/检查清单/复盘 5 份、review-template 与模板合集的复盘模板合并为 0302 的 `复盘模板.md`。
- 撞号处理：旧目录先 `mv` 到 `tmp-*` 再建新目录，迁完删除，无残留。
- 同步引用：AGENTS.md（bn 编号表 + 阶段语义改为按交互回合）、docs/workspace.md、wb-0001-router（路由表+复盘 skill 名）、beannote README 与 content-playbook、MEMORY.md。历史任务文档里的旧名按约定保留。
- 待确认项按方案默认执行：成稿留 01 阶段收尾、复盘放 03 条件触发、全自动模式可走到审稿结束但发布必须停。
- `make check` 3/3 通过。未提交。

## topics 月份归档（已执行）
- `topics/` 根只留当前月（2026-09）选题，往月按**选题记录日**搬进 `topics/YYYY-MM/`：05 月 25 篇、07 月 4 篇、08 月 1 篇，根剩 1 篇。git mv 全识别为 rename。
- 规则写进 `bn-tools-content-context`（新增「月份归档」+「归档时机自查」段）：月份按记录日算不按发布日；当前月新开题直接建根，不预判；归档只换目录不改目录名；触发时同步索引目录列。
- 同步：`bn-0000-topic-flow` 开题动作加归档检查步骤、`bn-0301-publish-flow` 改名步骤标明月不改、`bn-0302-retro-flow/references/复盘模板.md` 输入路径加 `[YYYY-MM/]`、beannote `README.md`（顺手删了重复的「目录结构」段）、`docs/项目说明.md`。
- `topics/索引.md` 重建，目录列带 `YYYY-MM/` 前缀；生成脚本 `/tmp/gen_index2.py`（盘点槽位时取文件名前 2 位数字）。
- 未提交。跨月后首次开题会自动触发归档，无需手工记。

## 每篇加 manifest.yaml（已执行）
- 用户要求每 topic 加一个状态文件，且要 yaml/json 格式（不要 md）。生成 `business-repo/beannote/topics/**/manifest.yaml` 共 31 篇，字段：`topic/status/platform/form/created/published/slots/next/conclusion/decisions/blockers/metrics/links`。`status` 与目录名前缀必须一致。
- 索引改为**由 manifest 派生**：`topics/索引.md` 不手改，改完 manifest 重跑生成脚本。脚本固化到 `business-repo/beannote/script/gen_index.py`（自带无依赖 YAML 解析，不依赖 pyyaml），并在 `bn-tools-content-context` 索引段指明路径。
- 模板 `bn-tools-content-context/references/manifest模板.yaml`。content-context 新增「manifest」段：字段含义表 + 6 条维护规则（manifest 权威、status 一致、抽不到留 null、只记判断不复制正文、被否切口必进 decisions、改名归档不动内容）。
- 6 个阶段 skill 各自接上 manifest 维护动作：开题写初始版、讨论换态度版结论+追加 decisions、成稿/审稿补 slots 与 next、发布改 status+published、复盘填 metrics。
- `docs/项目说明.md` 增 manifest 说明（只描述项目，不含操作规范）；删除早期 `docs/目录规范.md`（规范全进 skill）。
- 校验：31 个 manifest 全部合法 YAML、字段齐全、status 枚举一致；`make check` 通过。未提交。

