# 2026-07-11 工作日志

- 纠正边界模块文档位置/格式：之前误放 `docs/边界模块-前端设计文档.md` 与 `docs/边界模块-后端设计文档.md`（根目录 + 拆两份，均错）。正确范式是 **一篇合并文档** 放 `docs/feature/`，命名 `2026-07-11_边界功能方案.md`，同时含前端改动点（uni-dev-flow）与后端改动点（backend-superone-dev-flow），末尾带「测试方案」三表。
- 已删除根目录两份错误文档，按兄弟文档（约定/刻度方案）范式重写一篇合并的 `docs/feature/2026-07-11_边界功能方案.md`：功能定位、目标和边界、两份状态模型、一隅入口、边界页 UI/UX（分段切换+右置表态按钮+兼容概览 chip）、boundaries 表（partner_reaction 列）、5 个 API（list/create/update/delete/react，走 /api/so/carbon/boundary/*）、前端/后端改动点、验收标准、推荐实施顺序、测试方案。
- 关键设计规范回顾：边界=两份模型（底线/喜欢/不喜欢），对方只读可表态（懂了/还需磨合）；兼容概览=前端 computed 派生计数，不单列接口、不弹通知；品牌禁红绿、用暖色点亮 vs 暖灰未点亮；入口在「一隅」卡片网格（非 rail tab）。
- 已把「carbon 文档与 dev-flow 规范」写入项目长期 MEMORY.md，避免再踩。

## 边界文案方向强化（用户要求）
- 从"相处卡片"改为锚定**"个人使用说明书"**隐喻。两文档均已更新：功能定位（边界就是两人的个人使用说明书）、命名理由（保留模块名"边界"、文案锚定说明书）、洞察、入口卡片 desc（"怎么和我相处，一份使用说明书"）、页眉标（"关于我，一份使用说明书"）、分段（"我的说明书 / Ta 的说明书"）、空态邀请卡（"每个人都需要一份说明书…"）、与现有面板边界说明。
- 关键判断：「使用说明书」隐喻比直说"底线"更软、更不审问（自带自嘲的亲密感），反向消除了之前"不叫说明书以避审问感"的顾虑。模块名"边界"保留。

## 边界功能开发落地（用户授权"开始开发吧，前后端Devflow"）
- 后端（此前完成，本轮复核 `go build` 通过）：表模型 `carbon_boundary_tab.go`、枚举 `constant/carbon.go`、DDL `scripts/migration/sql/20260711_create_carbon_boundary_table.sql`、DTO、ICarbonRepo+factory+service（含权限校验）、use_case、router（5 接口 POST 于 `/api/so/carbon/boundary/*`）。
- 前端（本轮完成，`npm run type-check` 通过）：`types/api/carbon.ts`（wire 用 number：category 1/2/3、partner_reaction 0/1/2 + *_text）、`types/carbon.ts`（CarbonBoundary 实体 + toCarbonBoundary/分类反应转换）、`apis/carbon.ts`（5 函数）、`composables/useBoundary.ts`（my/partner 列表、struggleCount 派生、表态乐观更新+回滚）、`biz-play-tab.vue`（加「边界」卡片 type page 走 navigateTo）、`pages/boundaries/index.vue` + `styles/06-pages/p-boundaries.css`（双视角分段/三分类/右置懂了·还需磨合/兼容概览 chip/冷启动邀请卡/原生增删改；暖色 token、琥珀点亮、双主题、无红绿）。pages.json + index.css 已注册。
- 契约（task #5）：首次创建 `business-repo/frontend-contracts/openapi/carbon_api.yaml`（此前该目录不存在），补 boundary 5 接口 OpenAPI 3 契约，与实际 DTO 对齐。
- DDL 执行：用户授权"继续"后执行到 test 共享库，`verify-carbon` 已加 `carbon_boundary_tab` 到 `carbonTables` 列表并确认 `exists, columns=9, indexes=4, rows=0`。注意 test 库是写死的远程共享库，建表真实生效，须先确认再动。
- 踩坑修复：① 类型返回应写字面量联合类型（`BoundaryCategory = 'bottom_line'|...`）而非宽松 string；② 页面里 `getPartnerOpenid` 需从 carbon store 暴露（原未导出），已补。
