# 2026-07-12 工作日志

## carbon 边界表上线 live
- 将 `scripts/migration/sql/20260711_create_carbon_boundary_table.sql` 执行到 **live 环境**（`weiyi_superone_db`，8.155.38.83）。
- 命令：`go run ./scripts/migration -action=exec-sql -sql-file=./scripts/migration/sql/20260711_create_carbon_boundary_table.sql -env=live`（在 backend-superone 模块根目录执行）。
- SQL 为 `CREATE TABLE IF NOT EXISTS`，幂等、无 drop/覆盖风险。
- 复验：`go run ./scripts/migration -action=verify-carbon -env=live` → `carbon_boundary_tab: exists, columns=9, indexes=4, rows=0`，与 test 端结构一致。
- 注：live 走 `DB_NAME = weiyi_superone_db`，test 走 `TEST_DB_NAME = weiyi_superone_db_test`；`carbonTables` 切片已含 `carbon_boundary_tab`，verify 能正确覆盖。
