# AGENTS.md

## 命名原则

Agent 指南文件和相关文档应使用中性、通用的名称。

优先使用以下文件名：

- `AGENTS.md`
- `.agents/skills/*`

避免使用带有平台标识的名称，除非该文件本身就是为某个平台定制的。

## 这样做的原因

- 让工作台在不同工具和 Agent 运行时之间保持可移植性。
- 让这些说明更像项目文档，而不是某个产品的品牌附件。
- 当同一个仓库被不同自动化入口使用时，减少不必要的改动。

## 实践规则

如果文件是为了指导 Agent 行为，就应该按它在工作台里的职责命名，而不是按读取它的工具命名。

## Skill 命名规则

`.agents/skills/` 下的 skill 目录名统一使用「族群前缀 + 四位编号 + 能力后缀」的编号式命名，默认格式如下：

```text
{族群前缀}-{阶段号}{阶段内序号}-{能力后缀}    阶段 skill
{族群前缀}-tools-{能力后缀}                  工具 skill，不占阶段号
```

命名约束：

1. `族群前缀` 必填，小写英文连字符，用于表达 skill 所属的族群。
2. `四位编号` 必填，拆成两个两位段，中间不加分隔符：
   - 前两位是 `阶段号`，代表该 skill 处在流水线的哪个阶段。
   - 后两位是 `阶段内序号`，`00` 留给该阶段的总控或入口 skill，`01` 起为阶段内的具体 skill。
3. `能力后缀` 必填，用于表达这个 skill 解决什么动作或流程，如 `plan-flow`、`api-flow`、`verify-flow`、`knowledge-base`。
4. 工具 skill 使用 `{族群前缀}-tools-{能力后缀}`，不属于任何阶段，供所有阶段调用，如 `dev-flow-tools-db-query`。
5. 统一使用小写英文和连字符，不使用空格、下划线、拼音缩写混搭或品牌式命名。
6. 避免直接使用工具品牌、Agent 品牌、仓名或个人化命名；除非该 skill 明确只服务某个平台适配层。
7. 目录名、`SKILL.md` 的 `name` 字段、以及所有文档中的引用三者必须完全一致。

族群划分：

- `dev-flow-*`：跨端开发流水线，即一次需求从方案设计到整体验证的完整链路。前后端各占一个阶段位，不按端拆成两个族群；carbon 品牌特化层并入本族。
- `bn-*`：beannote 内容生产线，即素材整理、内容创作、发布复盘等内容能力。
- `wb-*`：工作台基建，即工作台入口、路由和上下文编排能力。

阶段划分：

`dev-flow` 按一次需求的推进顺序划分四个阶段：

| 阶段号 | 含义 | 进入条件 | 产物 |
| --- | --- | --- | --- |
| `00` | 方案设计 | 任意开发需求进入 | 方案文档 `task/YYMMDD_{主题}/README.md` |
| `01` | 后端开发 | 方案已确认且涉及后端改动 | 领域层、DDL、接口、注入、契约 |
| `02` | 前端开发 | 方案已确认且涉及前端改动 | 类型、API、状态、页面、样式、资产 |
| `03` | 整体验证 | 01 / 02 已产出 | 验证结论、方案回填 |

阶段可跳过：纯前端需求跳过 `01`，纯后端需求跳过 `02`。只有 `00` 和 `03` 是必过节点。

`bn` 按用户交互回合划分：**内容线不是流水线**。dev-flow 的阶段按技术分工切、门禁靠命令判定、可全自动跑完；内容创作里 agent 无法自己判定好坏，质量判定权在用户手上，所以 bn 按「用户在这一步说什么、agent 怎么接」切阶段，每步都要停下等用户一句话。`wb` 按工作台调用链路划分：`00` 入口、`01` 路由与维护。

实践规则：

- 编号按开发顺序分配，不看重要性：阶段内总控固定为 `xx00`，具体 skill 从 `xx01` 起顺排。
- 新增 skill 先定阶段、再定阶段内序号，默认追加到该阶段末尾，不插队、不复用退役编号。
- 前后端可能出现同名能力（如两端都有 `api-flow`），靠阶段号区分，不改后缀；编号表里要写清各自职责。
- 新增长在多个阶段被调用、又不专属任一阶段的横切能力时，建 `*-tools-*` 工具 skill，不要塞进阶段编号里。
- 确实需要插入到阶段中间时，必须重排该阶段后续序号，并同步更新 `SKILL.md` 的 `name`、所有文档引用和 `script/check-workbench.py` 里的路径清单。
- 同一族群内的 skill 命名要保持平行结构，例如 `dev-flow-0000-plan-flow`、`dev-flow-0103-api-flow`、`dev-flow-0301-verify-flow`。
- skill 数量要收敛：同阶段内总是连着走、且单次都只有几十行的小 skill 应合并成一个，用章节区分职责；单个 skill 超过 300 行才考虑拆分。合并后阶段内序号重排，同步所有引用。
- 业务仓内沉淀出的可复用 skill，稳定后应迁移到 `.agents/skills/`，按族群归入编号体系，并移除带有平台或个人品牌色彩的目录命名。

## 当前 skill 编号表

### dev-flow 跨端开发

阶段 00 方案设计：

| 编号 | 职责 |
| --- | --- |
| `dev-flow-0000-plan-flow` | 需求理解、改动范围确认、方案文档产出、阶段派发 |

阶段 01 后端开发：

| 编号 | 职责 | 顺序 |
| --- | --- | --- |
| `dev-flow-0101-domain-flow` | 领域层 entity/model/factory/repo/service + 字段设计与 DDL/DML SQL、GORM 表模型（只设计，不连库） | 1 |
| `dev-flow-0102-db-change-flow` | 迁移脚本、test/live DDL/DML 执行与验证 | 2 |
| `dev-flow-0103-api-flow` | 后端 DTO/UseCase/Service/路由 + Wire 依赖注入装配 | 3 |
| `dev-flow-0104-contract-flow` | OpenAPI YAML 与前端契约文件生成 | 4 |

阶段 02 前端开发，交接物是 `dev-flow-0104-contract-flow` 输出的前端复制清单：

| 编号 | 职责 | 顺序 |
| --- | --- | --- |
| `dev-flow-0201-archetype-flow` | 产品原型判断、信息骨架、CTA 结构 + 状态矩阵补全（硬门槛） | 1 |
| `dev-flow-0202-data-flow` | 前端 API 类型与请求封装、可选 Repo、Pinia store 与 Composable | 2 |
| `dev-flow-0203-page-flow` | 页面、路由与生命周期 + 组件与样式审美执行 | 3 |
| `dev-flow-0204-asset-flow` | SVG 资产生产与资产主权 + carbon 品牌图标（命中品牌项目时叠加） | 4 |
| `dev-flow-0205-space-ui` | carbon 品牌 UI 规范（命中 carbon 时全程叠加） | 叠层 |

阶段 03 整体验证：

| 编号 | 职责 |
| --- | --- |
| `dev-flow-0301-verify-flow` | 后端 gofmt/wire/test/lint 与审查、前端 type-check/build 与交付验收、契约核对、方案文档回填 |

工具层 `dev-flow-tools-*`，不占阶段号：

| 编号 | 职责 | 被谁调用 |
| --- | --- | --- |
| `dev-flow-tools-repo` | 多仓目录约定、技术栈、构建命令、分支推进与部署 | 所有阶段 |
| `dev-flow-tools-db-query` | 只读数据库排查与结构验证 | 00 / 01 / 03 |

### bn 内容生产

| 编号 | 职责 | 阶段 |
| --- | --- | --- |
| `bn-0000-outline-flow` | 选题+讨论合一：素材检索、反问、选题排雷、平台判断、建目录；模式判断、总体大纲（选题判断+七字段+写法方向）、走偏自查、确认门 | 00 选题与大纲 |
| `bn-0101-draft-flow` | 成稿：大纲确认后一次成稿、选题判断落正文开头、摊稿点出拿不准处 | 01 成稿 |
| `bn-0201-review-flow` | 审稿：接反馈改稿、规则沉淀、发布检查清单、逐轮收敛 | 02 审稿 |
| `bn-0301-publish-flow` | 发布：草稿箱推送、登记发布数据、目录加 `done-` | 03 发布 |
| `bn-0302-retro-flow` | 复盘：读者收获感评分（读得下去/有收获感/被尊重，不评传播量）、生产复盘、规则沉淀、自进化判断（条件触发） | 03 发布 |

工具层 `bn-tools-*`，不占阶段号：

| 编号 | 职责 | 被谁调用 |
| --- | --- | --- |
| `bn-tools-knowledge-base` | 书籍 / 演讲 / 访谈 / 股东信整理为知识库条目，存 `knowledge-base/`；只整理不生产 | 00 / 01 / 03 |
| `bn-tools-content-context` | beannote 目录结构、槽位编号、状态前缀、索引维护、关键路径、规则沉淀去向表、通用交互规则 | 所有阶段 |
| `bn-tools-writing-rules` | 创作准则入口、账号表达写法、公众号一次可发布标准 | 01 / 02 |

### wb 工作台基建

| 编号 | 职责 | 阶段 |
| --- | --- | --- |
| `wb-0001-router` | 工作台入口与路由（建立上下文 + 业务仓路由 + 开发需求硬路由） | 00 入口 |
| `wb-0101-skill-refactor` | skill 族群阶段化重构（定阶段、拆总控、建工具层、重排编号、合并精简、同步引用） | 01 路由与维护 |

## 工作台入口

- [工作台说明](README.md)
- [工作台规范](docs/workspace.md)
- [自进化机制](docs/self-evolution.md)
- [任务管理规范](docs/task-system.md)
- [目标管理](docs/goals.md)
- [知识库规范与条目模板](knowledge-base/README.md)

## 工作台规则

1. 工作台内的所有 `md` 文档，包括 `skill` 相关文档，统一使用中文；专业术语和业务术语可以保留英文。
2. 除非是明确的业务需求，否则不应该直接改动业务仓内容。
3. 工作台用于沉淀个人 `skill`、知识库、任务记录和脚本，并承担业务仓之间的全局路由、上下文聚合和跨仓编排职责。
4. 完成业务需求后，如果出现可复用经验、反复问题或意图识别失败，应反向优化工作台的 `skill`、知识库或脚本。
5. 新增或接入业务子仓时，必须先做工作台级整体检查：同步更新 `business-repo/` 索引、路由规则、相关 `skill`、任务登记和知识库入口；必要时补充专属工作流或沉淀记录，不能只加子模块不改工作台引用。
6. 新建任务文件夹前，先检查最近的任务目录和 `task/registry.md`；如果任务属于同类问题、零碎子任务或同一上下文链条，优先合并到已有任务目录继续共用上下文，避免拆成多个孤立任务。

## 目录职责

- `.agents`：跨项目复用的 skill 实体目录，由工作台 git 做版本管理；模板、清单和工作流放各 skill 的 `references/`。home 目录下的 `~/.agents` 是指向它的外链，详见 [跨项目共享](#跨项目共享)。
- `docs/`：工作台说明类文档，平铺不分层。
- `knowledge-base/`：业务仓、系统和公共知识沉淀，含知识库规范与条目模板。
- `task/`：工作台任务记录、热任务索引、上下文登记和痛点记录。
- `script/`：工作台常用脚本。
- `business-repo/`：业务仓集合，包含后端、前端、协议等独立仓；只在明确业务需求时改动。

## 跨项目共享

skill 实体就在工作台的 `.agents/skills/`，跟着工作台一起提交、一起回滚，不做独立仓库。`~/.agents` 是指向它的外链，其他 Agent 工具读 home 目录下的 `.agents` 就能拿到同一套 skill：

```bash
ln -s /Users/bean/workspace/bean-workbench/.agents ~/.agents
```

某个项目想在本仓目录里也看到，再建一条项目内链接 `ln -s ~/.agents /path/to/project/.agents`；摘除用 `rm /path/to/project/.agents`，**不要带尾斜杠**，否则会顺着链接删到实体。

三条约定：

1. 改 skill 就是改工作台文件，`make commit` 能带走，不需要单独提交；`make check` 会提示 `.agents/` 下有没有漏掉的改动。
2. 改动全局生效，所有读 `~/.agents` 的项目和工具都会看到最新版；只想在工作台内试的改动，用 `git stash push -- .agents` 暂存。
3. 接入约定写在 `.agents/README.md`，改链接机制时同步更新那份文档。

## 自进化触发条件

满足任一条件时，应考虑更新工作台沉淀：

1. Agent 连续两轮以上没有识别清楚用户意图。
2. 同一路径的操作、排错或验证问题多次复现。
3. 某个业务仓需求暴露出稳定可复用的流程、约束或知识。
4. 热任务需要更快路由到固定业务仓、文档或上下文。

更新优先级：先补 `task/registry.md` 或痛点记录，再沉淀到 `knowledge-base/`，最后在稳定后升级为 `skill` 或脚本。
